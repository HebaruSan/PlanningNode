KERBALCHANGELOG
{
	modName = Planning Node
	author = HebaruSan
	license = GPL-3.0
	website = forum.kerbalspaceprogram.com/index.php?/topic/200375-PlanningNode
	showChangelog = True
	VERSION
	{
		version = 0.1.2
		versionName = Life is What Happens
		versionKSP = 1.11
		CHANGE
		{
			change = Chinese localization
			type = Add
		}
	}
	VERSION
	{
		version = 0.1.1
		versionName = Contact with the Enemy
		versionKSP = 1.11
		CHANGE
		{
			change = Better input locking
			type = Fix
		}
		CHANGE
		{
			change = Marker color editing, defaulted from target orbit
			type = Add
		}
		CHANGE
		{
			change = Optimize trig calls
			type = Fix
		}
		CHANGE
		{
			change = Don't throw exceptions for normal maneuvers
			type = Fix
		}
		CHANGE
		{
			change = Don't randomly zoom in on the Sun
			type = Fix
		}
		CHANGE
		{
			change = Don't tab to ghost maneuvers in map view
			type = Fix
		}
		CHANGE
		{
			change = Update maneuver when switching bodies
			type = Fix
		}
		CHANGE
		{
			change = Make TS button responsive when all nodes are vessel-specific
			type = Fix
		}
		CHANGE
		{
			change = Rotate camera to look at markers on close
			type = Add
		}
		CHANGE
		{
			change = Make markers smaller in small SOIs and bigger in big SOIs
			type = Change
		}
		CHANGE
		{
			change = Show where off-screen markers are at screen edges
			type = Add
		}
		CHANGE
		{
			change = Remove Sun from list of allowed start bodies
			type = Fix
		}
		CHANGE
		{
			change = Button to warp to a planning node with a buffer
			type = Add
		}
	}
	VERSION
	{
		version = 0.1.0
		versionName = Ruthless Revision by Nature or Fate
		versionKSP = 1.11
	}
}
